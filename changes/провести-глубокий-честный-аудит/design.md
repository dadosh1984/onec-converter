# Дизайн — глубокий честный аудит (раунд 6)

## Что это
Не код-фича, а аудит проекта: глубокий, честный, проверенный по коду.
Результат — документ `docs/audit-round-6d.md`: реальные дефекты (A),
мёртвый код/дубли/хардкоды (B), функциональность (C), перф (D),
безопасность (E), UX (F), оптимизация команд (G), тесты/вороты (H),
документация (I) + поэтапный план реализации (Фазы 54–59).

## Метод
- Проверено по коду: cli.py, ai_skills.py, config.py, audit.py,
  mcp_server.py, source_8x_file.py, v77_reader.py, s3_client.py,
  load_8x.py, sql_source.py, terminal.py, doc-дизайн (round5, backlog).
- Честно: не выдумывать пункты ради «500». Реально найдено ~40 улучшений.
  Если проект «совершенен» в нише — сказать это прямо, а не плодить фикции.

## Подтверждения фактов
- 31 CLI-подкоманда (кода CLI перечисляет 31).
- `mcp`-подкоманда только СПИСЫВАЕТ тулы (`cmd_mcp`), сервер не запускает.
- Мёртвый код A1 подтверждён в ai_skills.py (del src_attrs).
- Дубль B5 подтверждён (cmd_dump_records vs cmd_export_xlsx).

## Верификация
Аудит считается завершённым, когда:
- документ создан и отражает фактический код (не выдумки),
- каждый [bug]/пункт снабжён местом по коду,
- план фаз 54–59 согласуется с реальными пробелами.
