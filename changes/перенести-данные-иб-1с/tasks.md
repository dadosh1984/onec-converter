# Задачи — перенести данные 1С 8.1 → 8.3 через MCP onec-converter

Перенос данных выполняется по алгоритму (плейбук) MCP-сервера onec-converter.
Каждый шаг вызывается через MCP-тул, результат фиксируется в result.md.

- [x] **Разведка источника и приёмника**
      `pipeline_status()` → `base_health(1C_8.1)` → `base_health(1C_8.3)` →
      `table_sizes(1C_8.1)` — объёмы, версия, структура.
- [x] **Сравнение структур и маппинг**
      `compare_structures(1C_8.1, 1C_8.3)` → `explain_diff(...)` →
      `auto_map_schemas(...)` — выявить совпадающие/различающиеся объекты
      и сгенерировать правила TOON (36 сопоставлено, 217 без соответствия).
- [x] **Выборочная проверка данных**
      `query_sql` — падал на дате-заглушке; диагностирован и исправлен
      (`decode_datetime` → None).
- [x] **Перенос данных в копию приёмника**
      `migrate --direct` (init→inspect→extract→named-map→transform→load):
      **4251 объект** записан в копию 1Cv8.1CD (`workdir_83/1Cv8.1CD`),
      `verify.ok=true`, `missing=[], mismatched=[]`.
- [x] **Сверка полноты и целостность журнала**
      `guid_diff(1C_8.1, 1C_8.3)` — совпадений GUID нет (разные конфигурации);
      перенос по именам (36 объектов) подтверждён.
- [x] **Фиксация результата и обновление проекта**
      `result.md` написан; обнаруженные дефекты исправлены; релизы 0.44.0/0.44.1
      опубликованы на PyPI/TestPyPI и GitHub Release; MCP-конфиг pi обновлён
      на `.venv/Scripts/onec-converter.exe`.
