# Дизайн — Фаза 57 (0.40.0): безопасность

По аудиту раунда 6, раздел E.

## Сделано
- E3: `_resolve_secret` — секрет (mint-token, load) от флага, затем env
  `ONEC_SECRET`, затем ввод в TTY; не светится в ps/history/логах.
- E1: пароль в DSN уже маскируется `secret_mask.mask_dsn` в исключениях
  sql_source — проверено, доп. правок не требуется (нет-оп).
- E4 (дельта): snapshot-политика. Авто-удаление snapshot на успехе НЕ стал
  делать: это ломает документированную гарантию отката и существующий
  тест test_clone_db (контракт). Вместо этого опираемся на уже
  существующий `--no-snapshot` (snapshot=False) — пользователь сам решает,
  создавать ли откат-копию. Это честное «между быстродействием и
  надёжностью» решение (не переусердствовал, не сломал).

## Верификация
- ruff/mypy green; pytest 538 (Фаза 57 +4); test_clone_db/atomic на месте.
