# Spec: core

## Назначение
Фаза 54 (0.37.0): исправить подтверждённые дефекты и вычистить мёртвый код из аудита раунда 6. Конкретно: A1 - убрать мёртвую переменную src_attrs/del в ai_skills.auto_map_schemas; B5 - вынести общий iter_table_jsonable из cmd_dump_records и cmd_export_xlsx, B6 - константа DEFAULT_SOURCE_ENCODING вместо хардкода 'cp866'. A2 - config.load: strip() всех строковых значений + булева коэрция. A3..A8: согласовать stdout/stderr в audit/verify/sonar, предупреждение при пустом --target в load (файловый режим), экранирование формул Excel (плюс E2). Не сломать проект: ворота green, Юнит-тесты на каждое изменение, быстрые тесты, баланс - не переусердствовать.

## Область

- В области: указанная возможность, поставляется тест-первой.
- Вне области: всё, что не заявлено в предложении.

## Критерии приёмки
- [ ] Заполнить в ходе реализации
