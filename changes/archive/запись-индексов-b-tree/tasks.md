# Tasks — запись индексов при append в 1CD (Фаза 14) — РЕВИЗИЯ (Вариант A)

РЕШЕНИЕ: запись индекса НЕ реализуется (image-формат не расшифрован,
валидация 1С невозможна). Фаза — документирование формата + тест-фиксация.
Ворота: mypy strict, ruff, pytest, vitest.

## GREEN — документация и тест-сниппет (без изменения поведения записи)
- [x] [spike] docs/format-8x.md: раздел «Индексы (Фаза 14, spike)» —
      объект индекса `index_page` без `1c fd` (FAT-объект), page0-каталог
      границ uint32, страницы описателей образов, image-страницы листьев
      (заголовок count:u16, битовая дельта-упаковка ключей, payload в хвосте),
      version-байты баз `01 00 00 00 20 00 00 00`, вывод: запись не безопасна
- [x] [fact] tests/test_8x_index_format.py: на копии `1C_8.1/1Cv8.1CD`
      (`_REFERENCE3`) или синтетике — читает каталог границ листьев
      (page0 uint32) через `_read_object_full(index_page)`, проверяет
      структуру (первый uint32=0, монотонный прирост), находит payload-ключ
      на последних байтах image-страницы — документирует формат, поведение
      append НЕ менять
- [x] [fact] `append_records` для таблиц с index_page!=0 — UserWarning
      «индексы не пересобираются» остаётся (тест уже есть:
      `test_write_8x_fl1.py::test_index_warning`) — не трогаем

## Постусловия / верификация
- [x] [assumption] pytest (все), ruff, mypy strict, vitest — зелёные
- [x] [assumption] README/docs — ограничение «индексы не пересобираются»
      подробно документировано в format-8x.md, поведение не изменено
